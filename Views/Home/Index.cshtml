@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    <div class="col-md-12">
        @if (Model != null)
        {
        <p>
            @Html.Raw((Model))
        </p>
        }
        else
        {

        }
    </div>
</div>
